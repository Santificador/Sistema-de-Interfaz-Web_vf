version: '3.8'

services:
  web:
    build: .
    container_name: seguroautopy
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    volumes:
      - .:/app
    command: python app.py
